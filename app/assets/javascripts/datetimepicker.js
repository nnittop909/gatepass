$(document).ready(function() {
  $('.datetimepicker').datetimepicker({
  	format: 'YYYY-MM-DD hh:mm A',
  	showClose: true,
      widgetPositioning: {
            horizontal: 'auto',
            vertical: 'auto'
      }
  });
});