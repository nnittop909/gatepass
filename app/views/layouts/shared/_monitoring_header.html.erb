<nav class="navbar navbar-inverse navbar-fixed-top " role="navigation" data-offset-top="60">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to monitoring_index_path, class: 'navbar-brand' do %>
        <div class="icon"><span class="fa fa-desktop"></span></div>
        <div class="title"> GatePass v.1.2.0</div>
      <% end %>
    </div>
    <div id="navbar" class="navbar-collapse collapse" aria-expanded="true">
      <ul class="nav navbar-nav navbar-right">
        <li>
          <% if user_signed_in? %>
            <%= link_to students_path, class: 'pull-right', title: 'Home' do %>
              <span><i class="fa fa-home fa-lg"></i> Home</span>
            <% end %>
          <% else %>
            <%= link_to new_user_session_path, class: 'pull-right', title: 'Administrator Sign-in' do %>
              <span><i class="fa fa-sign-in fa-lg"></i> Sign-in</span>
            <% end %>
          <% end %>
        </li>
      </ul>
    </div>
  </div>
</nav>